{
  "name": "typebox_concept",
  "version": "1.0.0",
  "description": "Signal K TypeBox validation library with smoke-test client",
  "type": "module",
  "main": "dist/lib/index.js",
  "types": "dist/lib/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/lib/index.d.ts",
      "import": "./dist/lib/index.js"
    }
  },
  "files": [
    "dist/lib"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "rm -rf dist",
    "generate:runtime-formats": "node staging/schemas/generate-runtime-format-module.mjs",
    "build": "npm run clean && npm run generate:runtime-formats && tsc -p tsconfig.json",
    "typecheck": "npm run generate:runtime-formats && tsc -p tsconfig.json --noEmit",
    "smoke": "tsx smoke-test-client/index.ts",
    "test": "npm run generate:runtime-formats && node --import tsx --test",
    "test:watch": "npm run generate:runtime-formats && node --import tsx --test --watch",
    "test:typecheck": "npm run generate:runtime-formats && tsc -p tsconfig.test.json --noEmit",
    "schemas:build": "node staging/converter/cli/build.mjs",
    "schemas:snapshot": "node staging/converter/cli/snapshot.mjs",
    "schemas:diff": "node staging/converter/cli/diff.mjs",
    "schemas:build:compare": "node staging/converter/cli/build-compare.mjs",
    "schemas:verify": "npm run test:typecheck && npm run test && node staging/converter/cli/check.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typebox": "^1.1.20"
  },
  "optionalDependencies": {
    "ws": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@types/ws": "^8.18.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
